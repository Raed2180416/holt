{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing: break the code deliberately and check the tests notice. A green suite proves the tests RAN; a mutation score proves they would have FAILED on a real defect. Scoped to the modules where a wrong answer is dangerous — analyze.mjs decides what is safe to delete, git.mjs decides what grove is allowed to run.",
  "packageManager": "npm",
  "testRunner": "command",
  "commandRunner": {
    "command": "node --test test/unit/safety.test.mjs test/e2e/detection.test.mjs test/e2e/break-it.test.mjs test/e2e/actions.test.mjs"
  },
  "mutate": [
    "src/analyze.mjs",
    "src/git.mjs"
  ],
  "reporters": ["progress", "clear-text", "json"],
  "jsonReporter": { "fileName": "reports/mutation.json" },
  "coverageAnalysis": "off",
  "timeoutMS": 120000,
  "concurrency": 4,
  "thresholds": { "high": 80, "low": 60, "break": null }
}
